document.addEventListener( "DOMContentLoaded", function( e ){
  Butter({
    modules: {
      timeline: {
      }
    },
    ready: function( butter ){
      butter.addMedia({});
    } 
  }); //Butter
}, false );
